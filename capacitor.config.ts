import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spinamp.retroplayer',
  appName: 'Spinamp Retro Player',
  webDir: 'dist'
};

export default config;
